version: '3.9'

services:
  db:
    image: docker.io/library/postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/setup.sql:/docker-entrypoint-initdb.d/01-setup.sql:ro
      - ./db/setup_tables.sql:/docker-entrypoint-initdb.d/02-setup_tables.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    image: localhost/summertask2026-api:latest
    build:
      context: ./api
      dockerfile: Dockerfile
      target: service
    environment:
      DATABASE_URL: ${DATABASE_URL_DOCKER}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8080:8080"
    command: uvicorn main:app --host 0.0.0.0 --port 8080

  web:
    image: localhost/summertask2026-web:latest
    build:
      context: ./web
      dockerfile: Dockerfile
      target: service
    working_dir: /app
    environment:
      VITE_API_BASE_URL: http://localhost:8080
    depends_on:
      api:
        condition: service_started
    ports:
      - "4173:4173"
    command: npm run dev -- --host 0.0.0.0 --port 4173
    volumes:
      - ./web:/app
      - web_node_modules:/app/node_modules

  grafana:
    image: docker.io/grafana/grafana-oss:11.5.2
    depends_on:
      db:
        condition: service_healthy
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_PATHS_PROVISIONING: /etc/grafana/provisioning
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro

volumes:
  pgdata:
  grafana-data:
  web_node_modules:
